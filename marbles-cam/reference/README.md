# Reference images

Drop 10 to 20 Juicy Marbles photographs in this folder. The M4 tuning screen will load them for side-by-side comparison and histogram matching against the section 1 targets.

Images placed here are gitignored on purpose: the look is versioned through the preset JSONs in `/presets`, not through the reference photos.
