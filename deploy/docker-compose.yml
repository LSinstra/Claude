services:
  apps:
    image: nginx:alpine
    container_name: claude-apps
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ..:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
